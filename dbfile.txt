import sqlite3
import os

dbs = [i for i in os.listdir() if i.endswith('.db')]
dbs.sort(reverse=True)
        
conn = sqlite3.connect(dbs[0])
cur = conn.cursor()
for row in cur.execute('SELECT * FROM cfn'):
    print(row)
cur.close()
conn.close()